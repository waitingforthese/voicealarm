# Freeze Pane V1
Base: LifeAlarm V14.2.2.1 Framework Font Larger
Changes:
- Framework Home top header is fixed while cards scroll.
- Framework Detail top heading + Kundli button are fixed while reading content scrolls.
- Kundli button remains visible while scrolling Framework Detail.
- Main Home Screen Life Alarm header/master alarm/settings row is fixed while home content scrolls.
- Existing functionality preserved; no alarm/notification/Aaradhana logic intentionally changed.
